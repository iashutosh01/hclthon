# Server
server.port=8080

# MySQL - Bean & Brew Cafe
spring.datasource.url=jdbc:mysql://localhost:3306/barista?createDatabaseIfNotExist=true&useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
spring.datasource.username=root
spring.datasource.password=ashutosh216
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# JPA / Hibernate
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=false
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect

# Simulation config (business rules)
coffeeshop.simulation.lambda=1.4
coffeeshop.scheduler.interval-seconds=30
coffeeshop.max-wait-minutes=10
coffeeshop.warning-threshold-minutes=8
coffeeshop.emergency-threshold-minutes=9.5
